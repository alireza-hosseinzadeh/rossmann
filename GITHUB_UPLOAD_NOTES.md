# GitHub upload notes

Suggested repository name:

```text
rossmann-sales-prediction
```

Suggested repository description:

```text
Rossmann store sales and customer prediction using Python, EDA, feature engineering, regression, tree-based models, boosting, and LSTM.
```

Suggested visibility: public only if you are comfortable sharing the notebook and report. Do not upload Kaggle raw data unless its terms allow redistribution.

Basic upload steps:

```bash
git init
git add .
git commit -m "Initial Rossmann sales prediction project"
git branch -M main
git remote add origin https://github.com/<your-username>/rossmann-sales-prediction.git
git push -u origin main
```
